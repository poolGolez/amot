import 'package:flutter/material.dart';

class AddPartakerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Participant'),
      ),
    );
  }
}
